# Plugah Orchestrator (Scaffold)

A minimal orchestrator that wraps **Plugah** to run:
1) Startup Discovery → 2) PRD → 3) Organization Planning (OAG) → 4) Execution.

## Install
```bash
# Using pip
pip install -e "."

# Or with uv
uv pip install -e ".[dev]"
